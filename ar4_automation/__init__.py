"""AR4 3D-printer automation library.

Import modules directly (e.g. ``from ar4_automation.printerclass import
BambuPrinter``). Nothing is re-exported here so that lightweight modules
like printerclass stay importable without pulling in the ROS stack.
"""
